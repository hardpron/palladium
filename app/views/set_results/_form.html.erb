<%= form_for(@set_result) do |f| %>
  <% if @set_result.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@set_result.errors.count, "error") %> prohibited this set_result from being saved:</h2>

      <ul>
      <% @set_result.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
