<p id="notice"><%= notice %></p>

<h1>Listing Set Results</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @set_results.each do |set_result| %>
      <tr>
        <td><%= link_to 'Show', set_result %></td>
        <td><%= link_to 'Edit', edit_set_result_path(set_result) %></td>
        <td><%= link_to 'Destroy', set_result, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Set result', new_set_result_path %>
